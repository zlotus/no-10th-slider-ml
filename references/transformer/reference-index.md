# Transformer.pptx 逐页语义索引

本索引基于逐页 XML 文字、19 张完整页面 PNG 和 6 个原始媒体文件人工核对生成。页面路径均相对于本目录。PPT 使用了分步动画，LibreOffice 静态导出只保留某个构建状态；Slide 009、016 尤其应结合 `text/` 与原始 PPTX 阅读。

## Slide 1

### 主题
封面：通俗解读《Attention Is All You Need》与 Transformer 架构。

### 本页核心内容
- 建立“人话版 Transformer”讲解定位。
- 封面由标题、论文页和网络结构图叠合而成。

### 重要视觉素材
- `pages/slide-001.png`
- `media/image1.png`、`media/image2.png`（原始封面拼贴素材）

### 值得复用的讲解思路
- “先解释为什么出现，再解释结构”这一通俗定位可保留；原页面拼贴较密，不建议直接复用排版。

### 可支持当前大纲
- 第 2 页：为什么从 Transformer 开始讲（定位素材）

### 备注
- LibreOffice 渲染出现动画对象叠合，原图更适合拆开复用。

## Slide 2

### 主题
讲解路线：从前代架构瓶颈到 Transformer。

### 本页核心内容
- 先讲前代序列模型的瓶颈，再引出 Transformer 的设计。
- 明确把 Self-Attention 作为理解 Transformer 的关键。

### 重要视觉素材
- `pages/slide-002.png`

### 值得复用的讲解思路
- 问题驱动的目录与本项目“上一代问题 → 新方法”的叙事一致。

### 可支持当前大纲
- 第 2 页：为什么从 Transformer 开始讲
- 第 3–4 页：RNN 到 Transformer 的转折

### 备注
- 页面中的 arXiv 链接可用于回查论文，不宜把目录页直接搬入新 Slide。

## Slide 3

### 主题
Sequence Transduction：序列到序列的任务定义。

### 本页核心内容
- 用机器翻译、文本生成、语音转文字说明“输入序列 → 输出序列”。
- 为 RNN、Encoder–Decoder 与 Transformer 提供共同任务背景。

### 重要视觉素材
- `pages/slide-003.png`

### 值得复用的讲解思路
- 用三个具体任务先建立问题，再进入架构，适合口头过渡。

### 可支持当前大纲
- 第 3 页：Transformer 之前，RNN / LSTM 如何处理序列
- 第 9 页：完整 Transformer，Encoder + Decoder

### 备注
- 这是任务背景页，不是架构原理页。

## Slide 4

### 主题
前馈神经网络（FNN）的基本结构。

### 本页核心内容
- 展示输入、隐藏层、输出的全连接映射。
- 为下一页说明 FNN 不适合可变长序列做铺垫。

### 重要视觉素材
- `pages/slide-004.png`
- `media/image3.png`（高分辨率网络结构图）

### 值得复用的讲解思路
- 将 FNN 作为“没有时间状态”的反例，帮助听众理解为何需要 RNN。

### 可支持当前大纲
- 第 3 页：RNN / LSTM 的前代背景（辅助）

### 备注
- 不属于 25 页大纲的核心页，适合作为讲稿背景而非单独成页。

## Slide 5

### 主题
FNN 处理序列的局限。

### 本页核心内容
- 向量求平均会丢失顺序信息。
- 直接拼接要求固定长度，且不擅长表达 token 间的序列关系。

### 重要视觉素材
- `pages/slide-005.png`
- `media/image3.png`

### 值得复用的讲解思路
- “平均会丢顺序、拼接会锁长度”的二选一对比很直观。

### 可支持当前大纲
- 第 3 页：为什么序列任务需要带状态的模型（辅助）

### 备注
- 该页只用于建立 RNN 的必要性，不要扩展成新的大纲分支。

## Slide 6

### 主题
RNN 的三项能力：顺序、上下文与可变长度。

### 本页核心内容
- RNN 顺序读取输入，因此计算路径天然包含位置。
- 隐状态传递上下文，可处理变长序列。

### 重要视觉素材
- `pages/slide-006.png`

### 值得复用的讲解思路
- 可改写为项目已确定的准确总结：“RNN 把顺序写进计算流程。”

### 可支持当前大纲
- 第 3 页：Transformer 之前，RNN / LSTM 如何处理序列

### 备注
- 原页的“序列信息”宜明确为顺序计算携带的状态，而不是保证长期记忆。

## Slide 7

### 主题
RNN 单步递归计算。

### 本页核心内容
- 用 `h_t = g(Wx_t + Uh_{t-1})` 展示当前输入与前一隐状态如何合并。
- 输出由隐状态映射得到，单元结构在时间步间复用。

### 重要视觉素材
- `pages/slide-007.png`

### 值得复用的讲解思路
- 把公式与单个 RNN cell 对齐，适合解释“后一步依赖前一步”。

### 可支持当前大纲
- 第 3 页：RNN 的顺序传播与串行依赖

### 备注
- 原页称激活函数可防止梯度爆炸/消失不准确；激活函数本身不能保证避免该问题，后续使用时应修正。

## Slide 8

### 主题
RNN 在翻译任务中的时间展开。

### 本页核心内容
- “我 爱 水 课”依次进入 RNN，隐状态逐步传递。
- 输出序列同样沿时间步产生，直观显示串行链路。

### 重要视觉素材
- `pages/slide-008.png`

### 值得复用的讲解思路
- 可把隐状态路径做成逐步高亮，用一张图同时说明顺序编码与并行瓶颈。

### 可支持当前大纲
- 第 3 页：RNN / LSTM 如何处理序列
- 第 4 页：为什么 Transformer 要解耦顺序与关系计算

### 备注
- 示例翻译只作机制演示，不代表严格的语言对齐。

## Slide 9

### 主题
Encoder–Decoder：允许输入和输出长度不同。

### 本页核心内容
- 编码器先读取输入，再由解码器生成输出。
- 解决简单逐时间步映射无法自然处理变长输出的问题。

### 重要视觉素材
- `pages/slide-009.png`
- `text/slide-009.md`（静态渲染未展开的文字）

### 值得复用的讲解思路
- “先压缩输入，再展开输出”是进入 seq2seq 的清晰过渡。

### 可支持当前大纲
- 第 9 页：完整 Transformer，Encoder + Decoder（历史背景）

### 备注
- 本页为动画构建页，PNG 近乎空白；必须结合文字与后续 Slide 10–11 查看。

## Slide 10

### 主题
Encoder–Decoder 的固定长度上下文向量。

### 本页核心内容
- 编码器将整句压到最后一个隐状态 `h4`，作为上下文 `C`。
- 这个信息瓶颈为后续 Attention 的出现埋下问题。

### 重要视觉素材
- `pages/slide-010.png`
- `media/image4.png`（编码器—解码器底图）

### 值得复用的讲解思路
- 把整句信息“塞进一个 C”作为直观瓶颈，适合引出长句遗忘。

### 可支持当前大纲
- 第 6 页：Attention 为什么出现（问题背景）
- 第 9 页：Encoder + Decoder

### 备注
- `media/image4.png` 被多页复用，可作为重绘底图参考。

## Slide 11

### 主题
固定上下文 `C` 驱动解码过程。

### 本页核心内容
- 同一个 `C` 在多个解码时间步被反复使用。
- 解码仍依赖前一步隐状态，保持递归式串行生成。

### 重要视觉素材
- `pages/slide-011.png`
- `media/image4.png`

### 值得复用的讲解思路
- 与 Slide 10 连续播放可说明“信息压缩”和“串行解码”两个不同瓶颈。

### 可支持当前大纲
- 第 6 页：Attention 的动机
- 第 9 页：Encoder–Decoder 数据流

### 备注
- 该架构是 RNN seq2seq，不应误标成 Transformer Encoder–Decoder。

## Slide 12

### 主题
Attention 的动机：长距离遗忘与信息重要性不同。

### 本页核心内容
- 固定向量难以承载长序列全部信息。
- 不同输入位置对当前输出的重要程度不同。
- 引出 Bahdanau Attention。

### 重要视觉素材
- `pages/slide-012.png`

### 值得复用的讲解思路
- 两个问题并列后，用“当前输出应该回看哪里？”自然进入 Attention。

### 可支持当前大纲
- 第 6 页：Attention，让每个 token 主动查找需要的信息

### 备注
- 这里讲的是 RNN Encoder–Decoder 上的跨注意力雏形，不是 Transformer Self-Attention。

## Slide 13

### 主题
Attention 权重示例：生成第一个输出时动态汇聚编码器状态。

### 本页核心内容
- `C0` 不再等于单个末状态，而是所有编码器隐状态的加权和。
- 权重代表当前解码步对各输入位置的相关程度。

### 重要视觉素材
- `pages/slide-013.png`
- `media/image4.png`、`media/image5.png`（结构底图与公式素材）

### 值得复用的讲解思路
- 用热度/线宽逐步改变权重，强调上下文会随输出位置变化。

### 可支持当前大纲
- 第 6 页：Attention 的“按需查找”直觉
- 第 9 页：Cross-Attention 的历史来源（辅助）

### 备注
- 此处 Query 来自解码状态，Key/Value 来自编码器状态；不要当成 Self-Attention 图。

## Slide 14

### 主题
Attention 权重示例：第二个输出位置。

### 本页核心内容
- `C1` 使用与前一步不同的权重重新汇聚输入。
- 展示注意力不是固定摘要，而是按解码步变化的检索。

### 重要视觉素材
- `pages/slide-014.png`
- `media/image4.png`、`media/image5.png`

### 值得复用的讲解思路
- 与 Slide 13、15 组成三帧动画，效果优于单独展示。

### 可支持当前大纲
- 第 6 页：Attention

### 备注
- 复用时应突出权重变化，避免堆叠全部公式。

## Slide 15

### 主题
Attention 权重示例：第三个输出位置。

### 本页核心内容
- `C2` 再次依据当前解码状态改变输入权重。
- 完成“每个输出位置各自查询输入”的序列演示。

### 重要视觉素材
- `pages/slide-015.png`
- `media/image4.png`、`media/image5.png`

### 值得复用的讲解思路
- 三页连续内容很适合拆为同一页的分步动画。

### 可支持当前大纲
- 第 6 页：Attention
- 第 20 页：Cross-Attention（概念类比，非现代文图结构图）

### 备注
- 原图用于解释加性/Encoder–Decoder Attention，不包含 Transformer 的 Q/K/V 矩阵细节。

## Slide 16

### 主题
Attention 之后仍存在的串行计算问题。

### 本页核心内容
- Attention 改善了信息瓶颈，但 RNN 编码和解码链仍是递归的。
- 为“抛弃循环结构、转向并行关系计算”做转折。

### 重要视觉素材
- `pages/slide-016.png`
- `text/slide-016.md`（静态渲染未展开的内容）

### 值得复用的讲解思路
- 强调“Attention 解决看哪里，尚未解决怎么并行”这一分工。

### 可支持当前大纲
- 第 4 页：Transformer 把顺序与关系计算解耦

### 备注
- 本页为动画初始帧，PNG 信息不完整；文字文件更可靠。

## Slide 17

### 主题
从 RNN 到 Self-Attention 的问题链。

### 本页核心内容
- RNN → Encoder–Decoder → Attention 逐项解决问题。
- 最后把“仍然串行”作为 Transformer / Self-Attention 的直接动机。

### 重要视觉素材
- `pages/slide-017.png`

### 值得复用的讲解思路
- 这是整份 PPT 最贴合本项目叙事的页面之一，可复用其“问题—方案—遗留问题”结构。

### 可支持当前大纲
- 第 3 页：RNN / LSTM
- 第 4 页：Transformer 的并行关系计算
- 第 24 页：十年演进时间线（早期节点）

### 备注
- 适合重绘为统一视觉语言，不建议直接复制原页面样式。

## Slide 18

### 主题
Transformer 前的架构演进与 Self-Attention 优势。

### 本页核心内容
- 横向比较 RNN、RNN + Attention、卷积序列模型、Memory Network 与 Transformer。
- 强调 Self-Attention 缩短任意位置之间的信息路径，并能以矩阵运算并行训练。

### 重要视觉素材
- `pages/slide-018.png`

### 值得复用的讲解思路
- “信息路径逐渐缩短”是比单纯说快更准确、更直观的演进线。

### 可支持当前大纲
- 第 4 页：顺序与关系计算解耦
- 第 6 页：Attention
- 第 8 页：Multi-Head Attention（仅概念背景）

### 备注
- 原页写 `Q*K` 应修正为 `QK^T`；“O(1)”指最大依赖路径长度，不是注意力计算复杂度；“完全并行”需限定为训练/编码侧，不能套到自回归推理。

## Slide 19

### 主题
RNN + Attention、Memory Network 与 Transformer 对比总结。

### 本页核心内容
- 以表格比较递归、注意力位置、并行性与序列结构。
- 试图总结 Transformer 用 Self-Attention 替代递归后的优势。

### 重要视觉素材
- `pages/slide-019.png`
- `media/image6.png`（原始对比表图）

### 值得复用的讲解思路
- “是否递归 / 注意力在哪里 / 能否并行”是可用的比较维度，但数据必须重做。

### 可支持当前大纲
- 第 4 页：Transformer 的并行关系计算
- 第 9 页：Encoder + Decoder（对照背景）

### 备注
- 原表把 RNN + Attention 标成“不递归”是错误；“Attention 只在 Decoder”与“Transformer 完全抛弃序列结构”也过度简化。应改为：Transformer 移除循环依赖，但仍用位置编码和掩码表达顺序；自回归解码推理仍然串行。

## 覆盖与缺口

- 已覆盖全部 19 页，且每页都映射到完整 PNG、逐页文本和可用媒体。
- 最值得优先复用：Slide 8（RNN 串行传播）、Slide 10–15（固定上下文到 Attention）、Slide 17（问题链）、Slide 18（信息路径演进）。
- 本资料没有足够的 Position Encoding / RoPE、标准 Q/K/V、Multi-Head、BERT/GPT 分流或 ViT 原图；后续实现大纲第 5、7、8、10、11 页时不能假装 PPT 已提供这些素材，应查 AI 生图索引或可靠论文官方图并自行准确绘制。
