# AGENTS.md

## 1. 项目目标

本项目用于制作一套关于 **近十年 Transformer 与文图生成算法演进** 的中文演讲 Slide。

整体内容分为两条主线：

1. Transformer：
   - RNN / LSTM
   - Self-Attention
   - Q / K / V
   - Multi-Head Attention
   - Position Encoding / RoPE
   - Encoder / Decoder
   - BERT / GPT
   - ViT / 多模态

2. 文图生成：
   - VAE
   - GAN
   - 自回归图像生成
   - CLIP
   - Diffusion / DDPM
   - Latent Diffusion / Stable Diffusion
   - Cross-Attention
   - DiT
   - Flow Matching / Rectified Flow
   - 现代多模态生成系统

最终目标不是制作“论文罗列型 PPT”，而是讲清楚：

> 上一代方法遇到了什么问题，新方法为什么出现，它解决了什么，又留下了什么问题。

---

## 2. 核心内容大纲

项目的主要内容规划见：

```text
transformer_image_generation_slide_outline.md
```

该文件是当前 Slide 叙事结构和页面内容的主要依据。

除非当前 Phase 明确要求修改，否则不要擅自重写、重排或大幅扩展该大纲。

如果实现过程中发现大纲存在技术性错误或明显叙事问题：

1. 先记录问题；
2. 在当前 Phase 的结果汇报中指出；
3. 不要未经确认直接改变整体结构。

---

## 3. 参考资料是主要内容来源

本项目已有两份核心参考资料：

```text
references/Transformer.pptx

references/AI生图范式演进与里程碑论文解读｜从VAE、GAN到 Diffusion、自回归.pdf
```

后续预处理后，应优先使用：

```text
references/transformer/
references/image-generation/
```

其中通常包括：

```text
pages/
media/
text/
reference-index.md
```

### 强制要求

在设计或实现任何具体 Slide 前：

1. 先查对应的 `reference-index.md`；
2. 再查看索引指向的完整页面 PNG；
3. 必要时查看对应 `media/` 原始素材；
4. 同时参考 `text/` 中的逐页文字；
5. 最后再结合模型自身知识补充或校正。

不要跳过参考资料，直接凭模型记忆重新写一套内容。

参考资料不是“可选背景材料”，而是本项目的主要讲解与视觉素材来源之一。

---

## 4. 参考资料的使用原则

优先复用参考资料中的：

- 优秀讲解顺序；
- 直觉化比喻；
- 模型结构图；
- 论文原图；
- Attention 可视化；
- 时间线；
- 生成效果对比；
- 高质量示意图；
- 有助于解释算法演进的文字。

但不要机械复制原 PPT / PDF 的完整页面设计。

### 应复用

- 信息结构；
- 图片；
- 图表；
- 有价值的原始视觉素材；
- 已经验证过的优秀讲解思路。

### 不应机械复用

- 原页面背景；
- 原页面排版；
- 原字体组合；
- 原页面视觉层级；
- 原 PPT 的整体 UI 风格。

最终 Slide 应保持本项目统一的视觉语言。

---

## 5. 内容准确性原则

技术准确性优先于“沿用参考资料原话”。

如果参考资料存在：

- 过时表述；
- 过度简化；
- 容易误导的类比；
- 与现代模型实现不一致的描述；

可以进行修正，但必须基于可靠的技术事实，而不是凭空改写。

不要为了让故事更顺而牺牲关键技术准确性。

---

## 6. Transformer 关键表述约束

以下理解已确定，后续不得退回到过度简化或错误表述。

### RNN / LSTM

RNN / LSTM 的序列顺序天然编码在顺序计算过程里：

```text
我 → 喜 → 欢 → 人 → 工 → 智 → 能
```

后一个时间步依赖前一个时间步。

因此：

> RNN 把“顺序”写进计算流程。

其代价是难以充分并行。

### Transformer

Transformer 将：

```text
序列顺序
```

与：

```text
token 之间的关系计算
```

解耦。

Self-Attention 可以通过矩阵运算并行计算整条序列中 token 的两两关系：

```text
Q("我 喜 欢 人 工 智 能")
            @
K("我 喜 欢 人 工 智 能")ᵀ
```

得到 Attention Matrix。

因此：

> Transformer 用矩阵乘法并行建模 token 之间的关系。

但 Self-Attention 本身不会天然提供完整的序列位置信息，所以需要显式的位置编码机制。

### Position Encoding / RoPE

原始 2017 Transformer 使用：

```text
Sinusoidal Positional Encoding
```

不是 RoPE。

RoPE 是后续广泛使用的位置编码方案之一。

不要写成：

> “Transformer 原本就是通过 RoPE 获得位置信息。”

更准确的总结是：

> **RNN：用时间传递位置。**  
> **Transformer：用位置编码描述位置，用矩阵乘法并行建模关系。**

---

## 7. 讲解风格

优先采用：

```text
上一代的问题
      ↓
为什么需要新方法
      ↓
新方法的核心直觉
      ↓
它解决了什么
      ↓
还留下了什么问题
```

避免：

- 一页塞很多论文；
- 大量公式推导；
- 纯定义式讲解；
- 名词堆砌；
- 为了“显得专业”增加无必要细节。

公式只在真正有助于理解时出现。

如果一个概念可以通过：

- 动画；
- 对比；
- 矩阵；
- 路径；
- 视觉隐喻；

解释清楚，优先使用视觉方式。

---

## 8. Slide 页面原则

默认每页只解决一个核心问题。

每页应尽量具备：

1. 明确标题；
2. 一个核心观点；
3. 一个主要视觉中心；
4. 少量辅助文字；
5. 必要时有一句页面底部总结。

不要把 Slide 做成文章截图。

不要把大量讲稿文字直接放进页面。

---

## 9. 动画原则

动画必须服务于理解，不是装饰。

适合使用动画解释：

- RNN 顺序传播；
- Q × Kᵀ；
- Attention Matrix 形成；
- Multi-Head 的不同关注关系；
- Encoder / Decoder 数据流；
- 图像 Patch 化；
- Diffusion 加噪 / 去噪；
- Cross-Attention；
- Noise → Data 的 Flow 路径；
- 时间线演进与两条技术路线汇合。

避免无意义的：

- 飞入；
- 旋转；
- 频繁缩放；
- 大量装饰性过渡。

---

## 10. 视觉素材原则

优先级：

```text
参考资料中的原始高质量图片
        ↓
参考资料完整页面中的可裁切内容
        ↓
论文官方图
        ↓
自行绘制结构示意图
```

如果已有高质量原图，不要无意义重新绘制一个更差的版本。

如果原图：

- 信息过密；
- 与页面视觉不统一；
- 只需要其中一部分；

可以重新绘制或拆解，但要保持原始信息含义准确。

---

## 11. 文图生成部分的叙事原则

不要把：

```text
VAE
GAN
Autoregressive
Diffusion
Flow
```

讲成五个互不相关的独立算法。

应始终强调它们之间的演进关系。

例如：

```text
VAE
↓
潜空间与生成表示

GAN
↓
高质量生成，但训练不稳定

Autoregressive
↓
把图像转成 token，用 Transformer 统一建模

Diffusion
↓
更稳定地学习生成分布

Latent Diffusion
↓
把 Diffusion 带入潜空间，降低计算量

DiT
↓
Transformer 成为生成主干

Flow
↓
进一步重新描述 Noise → Data 的生成路径
```

---

## 12. Transformer 与文图生成必须最终合流

整套演讲不能像两个独立主题拼接。

需要逐步让观众看到：

```text
Transformer
    ↓
ViT
    ↓
Multimodal Representation
    ↓
Cross-Attention
    ↓
DiT / MMDiT
    ↓
Modern Image Generation
```

最终核心关系：

> Transformer 主要解决表示与信息交互。

> Diffusion / Flow 主要解决生成过程。

现代多模态生成系统把二者结合在一起。

---

## 13. 分阶段开发

本项目严格按 Phase 推进。

每个 Phase 的具体任务由单独 Prompt 指定。

### 强制要求

当前 Phase 完成后：

- 停止；
- 自检；
- 汇报结果；
- 等待用户验收。

不要自行进入下一 Phase。

不要因为“顺手”提前实现未来阶段内容。

如果当前 Phase 只要求参考资料预处理，就不要开始做 Slide。

如果当前 Phase 只要求样板页，就不要批量完成全部页面。

---

## 14. 不要过度工程化

这是一个演讲 Slide 项目，不是大型 SaaS 系统。

避免：

- 无必要的复杂架构；
- 过多抽象层；
- 为可能永远不会出现的需求提前设计；
- 大量无意义配置；
- 不必要的状态管理框架；
- 为几张页面建立复杂 CMS。

优先：

- 简单；
- 清晰；
- 易修改；
- 易调试；
- 页面效果可控。

---

## 15. 文件与目录原则

不要修改或删除原始参考资料：

```text
references/Transformer.pptx

references/AI生图范式演进与里程碑论文解读｜从VAE、GAN到 Diffusion、自回归.pdf
```

自动生成的预处理内容应放在明确目录中。

辅助脚本统一放：

```text
scripts/
```

不要在仓库根目录随意堆放：

- 临时图片；
- 测试输出；
- 中间 PDF；
- 调试文件；
- 无意义日志。

---

## 16. 自动化脚本原则

如果需要编写预处理或构建脚本：

- 尽量幂等；
- 可重复执行；
- 使用项目相对路径；
- 不写死当前机器绝对路径；
- 不破坏原始文件；
- 失败时给出明确错误；
- 不静默吞掉异常。

如果依赖外部系统工具，应在 README 或相关文档中说明。

---

## 17. 不要伪造素材或来源

如果某张图片、论文图或参考内容找不到：

不要假装它存在。

不要编造：

- 页码；
- 图片来源；
- 论文图编号；
- reference-index 对应关系。

如果只能自行绘制：

明确把它当作本项目新绘制的示意图。

---

## 18. 阶段结束时的汇报

每个 Phase 完成后，至少汇报：

1. 本阶段完成了什么；
2. 新增或修改了哪些关键文件；
3. 是否有未解决问题；
4. 是否发现内容准确性问题；
5. 是否存在需要用户判断的设计选择；
6. 是否已满足本阶段验收标准。

不要在阶段汇报后自行继续开发。

---

## 19. 优先级

发生冲突时，优先级如下：

```text
当前用户明确指令
        ↓
当前 Phase Prompt
        ↓
AGENTS.md
        ↓
transformer_image_generation_slide_outline.md
        ↓
reference-index.md
        ↓
原始 PPT / PDF
        ↓
模型自身知识
```

其中：

- 原始参考资料是主要内容与素材来源；
- 但技术事实准确性不能因为参考资料表述而被牺牲；
- 如果发现冲突，应明确指出，而不是静默选择一种版本。

---

## 20. 最终原则

本项目不是为了证明“看过多少论文”。

最终目标是：

> **让一个具备基础 AI 概念、但并不熟悉底层算法的听众，在有限时间内理解 Transformer 与现代文图生成为什么会沿着这条路线发展到今天。**

所有内容、视觉、动画和代码实现，都应服务于这一目标。
